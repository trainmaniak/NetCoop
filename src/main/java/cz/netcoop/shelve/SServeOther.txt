package cz.netcoop.servingdaemon;

import cz.netcoop.*;
import cz.netcoop.Connector;
import cz.netcoop.exceptions.ConnectionException;

import java.io.IOException;

public class SServeOther extends AServeDaemon {

    private void foo() {

    }

    @Override
    public void run() {
        while (true) {
            try {
                Connector conn = getConnector();

                MessageOld messageOld = conn.receive(conn.getSessionList().get(0).getPortOther());

                //IDevice destination = ((AppNetCoopServer)getAppNetCoop()).getRouter().getDestinationDevice(messageOld);
                //conn.send(conn.getSession(destination).getPortOther(), messageOld);
            } catch (IOException | ConnectionException e) {
                e.printStackTrace();
            }
        }
    }
}
